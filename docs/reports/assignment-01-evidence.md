# Assignment 1 — Setup Evidence

Genuine Bellserver command output captured while building the Assignment 1
setup branch.

```text
$ python3.12 --version
Python 3.12.3

$ node --version
v22.22.2

$ git --version
git version 2.43.0

$ opencode --version
1.18.18
```

OpenCode was installed with:

```bash
npm install --global opencode-ai@1.18.18
```

No provider was configured and no provider credential was copied or recorded.

VS Code desktop evidence cannot be produced on Bellserver. The user must open the cloned
repository in VS Code on Windows and capture that genuine screenshot before submitting
Assignment 1. No screenshot is fabricated in this repository.
